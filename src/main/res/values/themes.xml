<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Usdlistener" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/silver</item>
        <item name="colorPrimaryVariant">@color/matt_white</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/gray</item>
        <item name="colorSecondaryVariant">@color/arsenic</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Background -->
        <item name="android:background">@color/white</item>
        <!-- Chart -->
        <item name="dataLineColor">@color/black</item>
        <item name="levelLineColor">@color/silver</item>
        <item name="textColor">@color/silver</item>
        <!-- Scrollbar thumb -->
        <item name="scrollbarThumbColor">@color/matt_white</item>
    </style>
</resources>